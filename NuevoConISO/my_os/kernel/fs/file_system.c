// file_system.c
#include "file_system.h"

void fs_init() {
    // Inicialización del sistema de archivos
}

int create_file(const char* filename) {
    // Crear un archivo (sin implementación por ahora)
    return 0;
}

int read_file(const char* filename) {
    // Leer un archivo (sin implementación por ahora)
    return 0;
}
